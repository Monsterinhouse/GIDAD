<Window x:Class="WPF_Test.Vista.EstadisticasWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sp="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        xmlns:local="clr-namespace:WPF_Test.Vista"
        mc:Ignorable="d"
        Title="Estadisticas" Height="500" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Hoy"            Click="BtnHoy_Click"    Padding="10,5" Margin="0,0,8,0"/>
            <Button Content="Últimos 7 días" Click="Btn7Dias_Click"  Padding="10,5" Margin="0,0,8,0"/>
            <Button Content="Último mes"     Click="Btn30Dias_Click" Padding="10,5"/>
        </StackPanel>

        <TextBlock x:Name="TxtTitulo" Grid.Row="1"
                   Text="Servicios realizados — Hoy"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <TabControl Grid.Row="2">
            <TabItem Header="Servicios">
                <sp:WpfPlot x:Name="PlotServicios"/>
            </TabItem>
            <TabItem Header="Totales (Efectivo / Seña / Tarjeta / Transferencia)">
                <sp:WpfPlot x:Name="PlotTotales"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
